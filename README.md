# pwa sample project

serve locally

    serve ./src

serve from firebase

    npm install -g firebase-tools
    firebase login
    firebase init
    firebase deploy

note: must update the firebase acct info before deploying